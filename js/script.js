$( function() {
// Draggable
$( "#draggable" ).draggable();

// Resizable
$( "#resizable" ).resizable();

//Selectable
$( "#selectable" ).selectable();

//Sortable
$( "#sortable" ).sortable();
$( "#sortable" ).disableSelection();

} );

	
